*.beam
*.ez
build/
erl_crash.dump

.direnv/

# Generated by `gleam run -m lustre/dev build` (built from client/).
# index.html is hand-owned (it links styles.css), so only the JS is ignored.
server/priv/static/client.js

# Nix build symlink
/result
